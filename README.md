# AI Telegram Moderator Bot

Этот проект представляет собой Telegram-бота для модерации чатов с использованием локальной языковой модели (LLM). Бот анализирует сообщения пользователей на соответствие заданным правилам и автоматически удаляет нарушения.

Проект оптимизирован для работы на потребительском оборудовании с NVIDIA GPU благодаря использованию квантизованной модели и библиотеки `llama-cpp-python`.

## 🚀 Особенности

- **Локальная работа:** Не требует подключения к платным API (OpenAI, Claude и др.). Вся обработка происходит на вашем устройстве.
- **Высокая скорость:** Благодаря полной выгрузке модели на GPU, вердикт выносится за 1-3 секунды.
- **Интеллектуальная модерация:** Способен понимать не только прямые нарушения, но и контекст, сарказм, пассивную агрессию.
- **Гибкая настройка:** Правила модерации легко изменяются прямо в Telegram через закрепленное сообщение.
- **Простота:** Код написан на Python и легок для понимания и модификации.

## 🛠️ Установка и настройка

### Шаг 1: Подготовка окружения

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/slam-ui/AI-Moderator-Bot.git
    cd AI-Moderator-Bot
    ```

2.  **Установите NVIDIA CUDA Toolkit:** Для работы с GPU необходим [CUDA Toolkit](https://developer.nvidia.com/cuda-toolkit-archive) (рекомендуется версия 12.1).

3.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv .venv
    ```
    Активируйте его:
    - Windows: `.venv\Scripts\activate`
    - macOS/Linux: `source .venv/bin/activate`

### Шаг 2: Установка зависимостей

1.  **Установите библиотеку Telegram:**
    ```bash
    pip install python-telegram-bot
    ```

2.  **Установите `llama-cpp-python` с поддержкой GPU:**
    - **Windows (PowerShell):**
      ```powershell
      $env:CMAKE_ARGS="-DGGML_CUDA=on"
      $env:FORCE_CMAKE=1
      pip install llama-cpp-python
      ```
    - **macOS/Linux:**
      ```bash
      CMAKE_ARGS="-DGGML_CUDA=on" FORCE_CMAKE=1 pip install llama-cpp-python
      ```

### Шаг 3: Загрузка модели

Этот проект оптимизирован для модели **Phi-3-mini-4k-instruct**.

1.  Скачайте файл модели `Phi-3-mini-4k-instruct-q4_K_M.gguf` с [Hugging Face](https://huggingface.co/microsoft/Phi-3-mini-4k-instruct-gguf).
2.  Поместите скачанный `.gguf` файл в корневую папку проекта.

### Шаг 4: Запуск бота

1.  Откройте файл `qwen_bot.py` и вставьте ваш токен от BotFather в переменную `BOT_TOKEN`.
2.  Запустите скрипт:
    ```bash
    python qwen_bot.py
    ```

## 📖 Использование

1.  Создайте группу в Telegram, добавьте в нее бота и сделайте его **администратором** с правом на удаление сообщений.
2.  Напишите в группе сообщение с правилами чата.
3.  **Закрепите** это сообщение.
4.  Отправьте в чат команду `/loadrules`.

Бот начнет модерацию всех последующих сообщений.
